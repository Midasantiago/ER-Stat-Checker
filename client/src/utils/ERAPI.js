// DISCONTINUED

/*import { gql } from '@apollo/client';

export const QUERY_WEAPON = gql`
    query {
        weapon (name: ${weaponName} ) {
            id,
            name,
            image,
            description,
            category,
            weight,
            requiredAttributes {
                name,
                amount
            },
            scalesWith {
                name,
                scaling
            }     
        }  
    }
`;*/